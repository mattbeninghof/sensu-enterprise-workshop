{
    "type": "Check",
    "spec": {
        "organization": "default",
        "environment": "default",
        "name": "check_sensu_io",
        "command": "check_http -H sensu.io -N",
        "runtime_assets": ["check_http"],
        "publish": true,
        "interval": 10,
        "subscriptions": ["docker"]
    }
}
